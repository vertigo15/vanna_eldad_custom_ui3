# Test dependencies. Install via:
#   pip install -r requirements-dev.txt
#
# `httpx` is used by FastAPI's TestClient. `pytest-asyncio` is wired so
# `async def test_*` functions Just Work without per-test boilerplate.
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
